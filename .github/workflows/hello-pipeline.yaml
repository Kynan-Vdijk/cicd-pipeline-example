name: hello runner

on:
  push:
    branches:
      - main

jobs:
  runner-job-definition:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Show runner info
        run: |
          echo "=== Current Directory ==="
          pwd
          echo
          echo "=== Directory Contents ==="
          ls
